{
  "name": "natsu-reader-js",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write src/ *.{js,mjs,css}",
    "format:check": "prettier --check src/ *.{js,mjs,css}"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.25.0",
    "eslint": "^10.4.1",
    "jsdom": "^26.0.0",
    "prettier": "^3.8.4",
    "vitest": "^3.0.0"
  }
}
